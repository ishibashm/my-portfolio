query Portfolios {
  posts(where: { categoryName: "portfolio" }) {
    nodes {
      title
      slug
      featuredImage {
        node {
          sourceUrl
          altText
        }
      }
    }
  }
}